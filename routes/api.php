<?php

use Illuminate\Support\Facades\Route;

// Route::prefix('admin')
// ->middleware('auth:admin')
// ->name('admin.')
// ->group(base_path('routes/api/admin.php'));

// Route::prefix('writer')
// ->middleware('auth:writer')
// ->name('writer.')
// ->group(base_path('routes/api/writer.php'));
